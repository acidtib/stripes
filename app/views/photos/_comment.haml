%li
  = link_to comment.user.username, profile_url(comment.user.id), { :title => "View #{comment.user.username}'s profile" }
  = comment.text
  %span.time<
    ="#{time_ago_in_words(comment.created_at)} ago"