%li
  %a.author{ :href => profile_url(comment.user.id), :title => "View #{comment.user.username}'s profile" }><
    = comment.user.username
  = ": "
  = raw parse_usernames(comment.text)
  %span.time<
    ="#{time_ago_in_words(comment.created_at)} ago"